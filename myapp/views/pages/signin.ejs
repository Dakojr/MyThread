<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head %>
    <link rel="stylesheet" href="stylesheets/style.css">
</head>

<body class="text-center">
    <form class="form-signin" method="post" action="/signin">
        <img class="mb-4" src="https://www.freeiconspng.com/uploads/letter-t-icon-png-12.png" alt="" width="100"
            height="100">
        <h1 class="h3 mb-3 font-weight-normal">Sign In</h1>
        <%if (hasErrors) { %>
        <div class="alert alert-danger">
            <% for(var i=0; i < messages.length; i++) {%>
            <p><%= messages[i] %></p>
            <% } %>
        </div>
        <% } %>
        <label for="email" class="sr-only">Email address</label>
        <input type="email" id="email" class="form-control" placeholder="Email address" name="email" required autofocus>
        <label for="password" class="sr-only">Password</label>
        <input type="password" id="password" class="form-control" placeholder="Password" name="password" required>
        </div>
        <input type="hidden" name="_csrf" value="<%= csurfToken %>"> <!-- Protect the page with a token -->
        <label>
            <input type="checkbox" value="remember_me" name="remember_me"> Remember me
            <!-- req.body.remember_me = NAME -->
        </label>
        <button class="btn btn-lg btn-primary btn-block" type="submit">Sign In</button>
    </form>
</body>

</html>